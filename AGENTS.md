# 协作约定

## 基本要求

- **始终使用中文交流**
- 编码是必须使用TDD流程，参考skills
- 文件命名使用小写烤串式
- 不要编写无意义的桥接函数、桥接类、桥接文件
- 不要把逻辑持续堆进单个大文件；优先拆成职责清晰的小模块
- 对复杂的代码添加必要注释；注释采用中文，注释应解释原因、约束或关键思路，不要解释显而易见的代码动作

## 实现原则

- 以生产标准为目标，不要为了迁就旧实现而写兼容层
- 不要用“再补一个判断”的方式修问题；先确认根因，再做结构性修复
- 一旦确定正确模型，就收敛到单一实现，不保留新旧双轨逻辑
- 优先复用 Cesium、MapLibre 及其子模块已经提供的能力、状态和语义，不要重复实现已有函数、机制等

## 修改策略

- 修复问题时优先解决根因，不要只修现场症状
- 不要盲改；如果无法稳定复现，先补测试或日志定位，再改实现
- 涉及缓存、生命周期、GPU 资源时，必须同时检查性能影响和内存泄漏风险

## 测试与校验

- 编写测试时，引用src目录必须用`@/`别名
- 修改代码后必须运行 `pnpm lint:eslint --fix` 和 `pnpm lint:tsc`
- 新功能必须补测试
- 修复 bug 必须补对应回归测试，确保问题不会再次出现

## 禁止事项

- 不要为了“先跑起来”而引入临时兼容代码
- 不要保留已经确认错误的数据模型
- 不要在没有验证的前提下持续叠加补丁
